# выведем длину кластеров
print(len(clusters))
# Если значение совпадет с кол-вом кластеров 
# в файле по условию, то аномалий нет.

# Если не совпадет, то запишем только те кластеры,
# кол-во точек в которых больше чем, например, 10
clusters = [cl for cl in clusters if len(cl) > 10]