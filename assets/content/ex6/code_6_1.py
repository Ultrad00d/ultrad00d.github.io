from turtle import *

screensize(3000, 3000) # увеличенный экран
tracer(0) # мгновенная отрисовка
m = 20 # <-- масштаб
left(90) # направление вдоль оси ординат

 ┌─────────────────────┐
 │ задание исполнителю │
 └─────────────────────┘

pu() # поднимаем перо, чтобы нарисовать точки
for x in range(-60, 60):
    for y in range(-60,60):
        goto(x * m, y * m) # идем в каждую
        dot(5)  # точку (x; y) и ставим dot(5)
done() # чтобы картинка не исчезла