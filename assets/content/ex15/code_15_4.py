# введем функцию ДЕЛ
def ДЕЛ(x, a): return x % a == 0

# введем функцию из условия
def f(x, a):
    return (ДЕЛ(405, x) <= ДЕЛ(81, x)) or (a - x > 162)

# создадим массив для возможных ответов
ans = []
# переберем ашки и иксы до 10 000
for a in range(1, 10_000):
    # и смотрим чтобы для всех иксов выполнялась истинность ф-ии
    if all(f(x, A) == 1 for x in range(1, 10_000)): ans.append(a)
# просили найти наименьшее a
print(min(ans))
# (можно было вывести первое найденное - оно было бы наим.)